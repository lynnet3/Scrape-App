<div class="card">
    <div class="card-body">
        <h3 class="card-title">
            Lets See the Latest News on The Onion. Cause we all need a little laugh.
        </h3>
    </div>
</div>
<div class="container">
    <div class="row">
        <ul>
        {{#each}}
        <h5>
            {{this.title}}
        </h5>
        <a href="/readArticle/{{this.id}}" class="btn btn-warning">Read Article</a>
        {{/each}}
        </ul>
        <div class="comments"></div>
    </div>
</div>